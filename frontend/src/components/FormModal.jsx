import React from 'react';
export default function FormModal({title,onClose,onSubmit,busy,error,children}){return <div className="modal-bg"><div className="modal"><div className="modal-head"><h2>{title}</h2><button type="button" className="icon-btn" disabled={busy} onClick={onClose} aria-label="Tutup">×</button></div><form onSubmit={onSubmit}>{error&&<div className="notice danger" role="alert">{error}</div>}{children}<div className="modal-foot"><button type="button" className="btn" disabled={busy} onClick={onClose}>Batal</button><button className="btn primary" disabled={busy}>{busy?'Menyimpan...':'Simpan'}</button></div></form></div></div>;}
