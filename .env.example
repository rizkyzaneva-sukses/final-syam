POSTGRES_DB=bos_syams
POSTGRES_USER=bos
POSTGRES_PASSWORD=local-development-only
# Optional in development; omitted secret is random and sessions reset on restart.
SECRET_KEY=
APP_ENV=development
SEED_DEMO=false
ACCESS_TOKEN_MINUTES=60
APP_PORT=8080
# Explicit demo data is available ONLY when APP_ENV=development and SEED_DEMO=true.
